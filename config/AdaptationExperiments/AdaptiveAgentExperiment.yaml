env: chain walk
norm: 1
num_iterations: 6100

agent_name: planner
get_optimal: False
meta_lr: 0.1

seed: -1
hydra:
  run:
    dir: ${oc.env:OUTPUT_DIR,./outputs}/${hydra.job.config_name}/${now:%Y-%m-%d}/${now:%H-%M-%S}
  job:
    chdir: True